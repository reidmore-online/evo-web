import type { WithNormalizedProps } from "../../global";
import { processHtmlAttributes } from "../../common/html-attributes";
static {
    interface FlagInput extends Omit<Marko.HTML.Span, `on${string}`> {
        flag?: string;
    }
}
export interface Input extends WithNormalizedProps<FlagInput> {}
$ const { flag } = input;

<span
    ...processHtmlAttributes(input, ["flag", "class"])
    class=["fflag", `fflag--${flag?.toLowerCase()}`, input.class]/>
